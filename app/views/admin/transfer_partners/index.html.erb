<div class="container">
  <div class="row">
    <div class="col-lg-12">
      <h2>Too Many Miles - Transfer Partners</h2><br>
      <ul>
        <% @transfer_partners.each do |partner| %>
          <li><%= partner.name %> | <%= link_to "Edit", edit_admin_transfer_partner_path(partner) %></li>
        <% end %>
      </ul><br>
      </br></br>
      <h3><%= link_to "Go Back to Main Admin Panel", admin_cards_path %></h3>
    </div>
  </div>
</div>

